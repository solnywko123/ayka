# DECISIONS.md — лог самостоятельных решений

Здесь фиксируются решения, принятые без явного указания в BRIEF.md, с обоснованием. Формат: дата, решение, почему.

## 2026-08-15 — Общий подход к выполнению

**Решение:** BRIEF.md выполняется как готовое ТЗ без дополнительного брейнсторминга с пользователем: раздел 17 прямо запрещает переспрашивать по мелочам, раздел 0 задаёт рабочие демо-данные, а раздел 18 задаёт порядок этапов. Решения по всем незакрытым вопросам принимаются самостоятельно и фиксируются здесь.

**Почему:** пользователь явно попросил не останавливаться с вопросами и работать по разделу 18 от начала до конца.

## 2026-08-15 — Тестирование без строгого TDD-цикла

**Решение:** код пишется, затем сразу покрывается тестами из раздела 15 (pytest для API, test_build.py и test_no_hardcoded_contacts.py для сайта), тесты прогоняются до зелёного состояния перед коммитом соответствующего этапа. Строгий red-green-refactor цикл по каждой мелкой функции не используется.

**Почему:** масштаб задачи — 14 этапов, полноценный сайт + API + админка за один проход. Формальный TDD на каждую строчку кратно увеличил бы время выполнения без выигрыша в качестве при таком объёме; при этом требование раздела 15 (тесты обязательны и зелёные) полностью выполняется.

## 2026-08-15 — Python окружение

**Решение:** используется `py` (Python Launcher, Python 3.13.4) вместо `python`/`python3` — на машине эти команды указывают на заглушку Microsoft Store. Виртуальное окружение `.venv` создаётся через `py -m venv .venv`. В Makefile и README команды используют `py`/`.venv` явно, без опоры на `python` в PATH.

**Почему:** `python`/`python3` в PATH ведут на неустановленную заглушку Windows Store и завершаются с ошибкой; `py`-лаунчер найден и указывает на реально установленный Python 3.13.4 (BRIEF.md требует 3.12+, 3.13 совместим).

## 2026-08-15 — Git identity

**Решение:** используется уже настроенная глобальная git-идентичность пользователя (найдена через `git config user.email/user.name`), локальный override в репозитории не создаётся.

**Почему:** git config уже содержит валидные global user.name/user.email, локальная переустановка не требуется и не запрашивалась.

## 2026-08-15 — `make` недоступен локально

**Решение:** `Makefile` создаётся по спецификации (раздел 3, 13, 14, 19.3) и будет работать в Linux/CI/деплой-окружениях (Netlify/Render, где `make`/POSIX-шелл есть по умолчанию). Локально на этой Windows-машине `make` не установлен, поэтому для самопроверки в ходе разработки команды из Makefile прогоняются напрямую (`py site/build.py`, `py -m pytest` и т.д.), а не через `make`.

**Почему:** `where make` не находит исполняемый файл на машине; переустановка make не входит в объём задачи и не блокирует выполнение остальных требований BRIEF.md.

## 2026-08-15 — Шрифт: Manrope

**Решение:** выбрана гарнитура Manrope (вариативный шрифт, SIL Open Font License 1.1 — свободное самостоятельное хостинг и распространение). Файлы `.woff2` скачаны с Google Fonts (одна вариативная версия на подмножество: latin, cyrillic, cyrillic-ext — они покрывают все нужные буквы, включая киргизские Ә/Ө/Ү/Ң) и лежат в `site/static/fonts/`, подключены через `@font-face` с `font-display: swap`, без CDN в проде.

**Почему:** раздел 4 требует гарнитуру с хорошей кириллицей на выбор (Inter/Manrope/Onest), self-hosted `.woff2`. Manrope даёт современный геометричный вид, хорошо читается в крупных заголовках с `clamp()`, лицензия позволяет самостоятельный хостинг.

## 2026-08-15 — Одинаковые URL-слаги во всех языках

**Решение:** структура путей (`/uslugi/{slug}/`, `/rayony/{slug}/`, `/ceny/`, `/o-nas/`, `/otzyvy/`, `/kontakty/`, `/blog/{slug}/`) одинакова для `ru`, `ky`, `en` — меняется только языковой префикс. Слаги не переводятся.

**Почему:** BRIEF.md даёт точные русские слаги только для `/ru/`, для `ky`/`en` слаги не оговорены. Единые слаги упрощают переключатель языка (раздел 5.5: «ведёт на ту же страницу в другом языке») и `hreflang`-разметку, снижают риск рассинхронизации при добавлении новых страниц.

## 2026-08-15 — Маппинг 6 услуг на 4 типа калькулятора

**Решение:** 6 страниц услуг используют 4 типа калькулятора (`maintenance/general/post_renovation/post_move` из `pricing.json`) так: генеральная→general, после ремонта→post_renovation, поддерживающая→maintenance, офисы→maintenance + property_type=office (предустановлено), химчистка мебели→maintenance + акцент на addon `sofa_seat`/`carpet_per_m2`, мытьё окон→maintenance + акцент на addon `windows_per_sash`.

**Почему:** в разделе 0 `pricing.json` заданы только 4 базовые ставки за м², а в разделе 5.2 перечислено 6 услуг — часть из них (мебель, окна) в реальности продаются как «допуслуги», а не отдельный тариф за м². Это соответствует и разделу 6 (окна/диван/ковёр — в списке «доп. услуги», а не типов уборки).

## 2026-08-15 — Добавлена страница-каталог `/uslugi/`

**Решение:** добавлена страница `/{lang}/uslugi/` со списком всех 6 услуг (шаблон `services_index.html`, контент — `pages.json → services`). В разделе 5 BRIEF.md такая страница отдельно не описана, но пункт меню «Услуги» в шапке (сквозной элемент, раздел 5.5) должен куда-то вести.

**Почему:** без этой страницы ссылка «Услуги» в шапке и хлебные крошки услуг вели в никуда — обнаружено автоматической проверкой битых ссылок по всей сборке (`site/dist`) после наполнения контента. Страница улучшает и внутреннюю перелинковку для SEO.

## 2026-08-15 — Проверка калькулятора/формы без браузера

**Решение:** визуальная проверка калькулятора и формы заявки в реальном браузере не выполнена — расширение Claude in Chrome не подключено в этой сессии, а иных браузерных инструментов нет. Вместо этого выполнена структурная проверка всей сборки: локальный HTTP-сервер поднят и отдаёт страницы (200 OK), автоматическая проверка отсутствия битых внутренних ссылок (2347 ссылок, 0 битых), валидности JSON-LD на всех 74 HTML-файлах, наличия title/description/canonical/ровно одного h1 на каждой странице (кроме технической `index.html`-редиректа). Формула `calculator.js` построчно сверена с формулой из раздела 6 BRIEF.md.

**Почему:** нужно быть честным о границах проверки — рантайм-поведение JS (реальный клик по калькулятору, реальная отправка формы) не подтверждено визуально в браузере, это должно быть перепроверено вручную владельцем или в отдельной сессии с подключённым браузерным инструментом перед стартом рекламы.

## 2026-08-15 — `from __future__ import annotations` несовместим с `@limiter.limit`

**Решение:** в файлах роутеров, где применяется декоратор `slowapi`'s `@limiter.limit(...)` (`api/app/routers/quote.py`, `api/app/routers/leads.py`), НЕ используется `from __future__ import annotations`.

**Почему:** при живом запуске обнаружено: с отложенным вычислением аннотаций (PEP 563) FastAPI пытается резолвить forward-ref параметров эндпоинта через `__globals__` обёрнутой декоратором функции — а `slowapi.Limiter.limit` подменяет функцию на обёртку, определённую в модуле `slowapi`, из-за чего `QuoteRequest`/`LeadCreate` там не резолвятся и приложение падает при старте с `PydanticUndefinedAnnotation`. Прямые (не отложенные) аннотации это обходят.

## 2026-08-15 — Относительный `DATABASE_URL` резолвится от `api/` как CWD

**Решение:** `DATABASE_URL=sqlite:///./dev.db` (не `./api/dev.db`) — предполагается, что `uvicorn`/`alembic`/`pytest` запускаются с рабочей директорией `api/` (как и написано в README: `cd api && ...`).

**Почему:** SQLAlchemy резолвит относительный путь sqlite от текущей рабочей директории процесса, а не от расположения файла настроек; единообразный CWD = `api/` исключает путаницу с `api/api/dev.db`.

## 2026-08-15 — Формула цены проверена end-to-end, но не через реальный JS

**Решение:** после поднятия API локально (`uvicorn`, SQLite) вручную сверены результаты `POST /api/v1/quote` и `POST /api/v1/leads` с ручным расчётом по формуле раздела 6 для нескольких наборов параметров (включая все множители и скидку) — совпадение точное. Формальное автоматизированное сравнение с `calculator.js` (раздел 15: «совпадение результата фронтового и бэкового расчёта») по-прежнему не может исполнить сам JS (нет Node.js и браузера в этом окружении, см. запись выше) — вместо этого `api/tests/test_pricing.py` (этап 12) будет тестировать `api/app/pricing.py` напрямую по значениям из общего `pricing.json`, а идентичность формул с `calculator.js` обеспечивается тем, что это один и тот же алгоритм, построчно перенесённый в оба файла (сверено вручную), с перекрёстными комментариями в обоих файлах.

**Почему:** прозрачность о том, что именно проверено автоматически, а что — только сверкой кода вручную.

## 2026-08-15 — Cookie `Secure` отключается только при `BUILD_ENV=dev`

**Решение:** флаг `Secure` на JWT-cookie админки (`api/app/security.py::cookie_kwargs`) выставляется в `True` всегда, кроме `BUILD_ENV=dev` — тогда `False`.

**Почему:** обнаружено при живом тестировании входа в админку: с `Secure=True` браузер (и `http.cookiejar` в тесте) не отправляет cookie обратно по обычному `http://`, а локальная разработка (в том числе на этой машине) идёт без TLS — вход в админку не работал вообще (редирект обратно на `/login` после успешной аутентификации). В проде (Render, HTTPS) `BUILD_ENV=prod` и `Secure=True` всегда, как требует раздел 7.

## 2026-08-15 — JSON API админки и серверный HTML используют общий `admin_service.py`

**Решение:** `api/app/routers/admin.py` (JSON API под `/api/v1/admin/*`, раздел 7) и `api/app/admin_ui/router.py` (серверный Jinja2-интерфейс под `/admin/*`, раздел 9) не дублируют логику и не дёргают друг друга по HTTP — оба вызывают общие функции `api/app/admin_service.py` (`list_leads`, `get_lead`, `update_lead`, `export_csv`, `compute_stats`), работающие прямо с ORM-сессией.

**Почему:** в разделе 3 структуры репозитория `routers/admin.py` и `admin_ui/` — разные файлы/модули, значит это разные вещи (JSON API и HTML-страницы), а не одно и то же под двумя именами; общий сервисный слой — стандартный способ не плодить дублирующуюся бизнес-логику.

**Проверено вручную** (локальный сервер, SQLite): логин через HTML-форму и через JSON `/api/v1/admin/login`, дашборд и список заявок рендерятся с реальными данными, смена статуса/заметки/ответственного через форму сохраняется и отражается в истории событий, экспорт CSV отдаёт корректные заголовки и все строки, `/api/v1/admin/stats` считает счётчики и потенциал верно, неаутentифицированный доступ к `/admin` редиректит на логин, к `/api/v1/admin/*` — 401 с единым форматом ошибки.

## 2026-08-15 — `test_no_hardcoded_contacts.py`: уточнение паттерна `wa.me/`

**Решение:** тест раздела 19.2 ищет не сам домен `wa.me/`, а признак захардкоженного номера сразу после него (`wa\.me/\d`) — потому что домен структурно обязан присутствовать в `header.html`, `footer.html`, `cta.html`, `contacts.html`, `form.js` для построения ссылки из `config.whatsapp` на лету. Также добавлен allowlist на `api/app/utils.py` — докстрока `normalize_kg_phone()` иллюстрирует принимаемые форматы номера (`'+996 555 12-34-56'` и т.п.), это документация формата, а не захардкоженный контакт компании.

**Почему:** буквальный grep по `+996`/`wa.me/` без уточнений ловит собственную инфраструктуру (домен WhatsApp) и документацию формата телефона как ложные срабатывания — цель раздела 19 (не дублировать РЕАЛЬНЫЕ контактные данные компании вне `config.json`) не нарушается ни доменом, ни примерами формата.

## 2026-08-15 — Блог и районы: длина текста доведена до минимума из BRIEF.md

**Решение:** после первого прохода несколько статей блога (все 3 языка) и часть страниц районов (`ru`/`ky`) оказались короче требований раздела 5.4 (1200+ слов на статью) и 5.3 (350+ слов на район). Тексты дополнены новыми содержательными абзацами (не «водой» — реальные подтемы: сравнение предложений компаний, типы ремонта и объём работ, кто убирает строймусор, red flags, статьи о конкретных районах), пока автоматический тест (`site/tests/test_build.py`) не стал зелёным на всех 9 статьях и 21 странице района (7 районов × 3 языка).

**Почему:** это прямое требование BRIEF.md, проверяемое тестом — расхождение обнаружено только после написания теста, что подтверждает ценность автоматической проверки поверх ручной оценки объёма текста на глаз.

## 2026-08-15 — Docker-образы не пересобраны через `docker build` (нет запущенного демона)

**Решение:** `api/Dockerfile`, `site/Dockerfile`, `docker-compose.yml`, `docker-compose.prod.yml` проверены через `docker compose config` (валидный YAML, все переменные и профили резолвятся, exit code 0) — это подтверждает синтаксическую корректность. Фактическая сборка образов (`docker build`) не выполнена: на этой машине установлен Docker Desktop, но его демон не запущен (`open //./pipe/dockerDesktopLinuxEngine: The system cannot find the file specified`), а запускать GUI-приложение без интерактивного участия пользователя — рискованное действие, которое не входит в объём этой задачи.

**Почему:** честность о границах проверки (см. также запись про калькулятор без браузера) — перед первым реальным деплоем стоит один раз прогнать `docker compose -f docker-compose.yml up --build` локально с запущенным Docker Desktop, чтобы убедиться, что образ API стартует и проходит health check.

## 2026-08-15 — Финальная проверка по разделу 16: что подтверждено автоматически, что нет

**Решение:** перед финальным коммитом прогнан полный набор автоматических проверок: `check_config.py` в dev (баннер) и в prod на копии конфига с реальными данными (ноль предупреждений — подтверждает, что раздел 19.3 действительно блокирует прод с плейсхолдерами и пропускает с реальными данными); `build.py` (69 страниц × 3 языка); `site/tests` (12/12) и `api/tests` (45/45) зелёные; сверка `.env.example` с `Settings` — все 12 полей совпадают; ручная структурная проверка 183 блоков JSON-LD на обязательные поля по типу (`HomeAndConstructionBusiness`, `Service`, `FAQPage`, `BreadcrumbList`, `Article`) — 0 несоответствий; `git log -p` на утечки секретов — чисто.

**Не подтверждено инструментально** (честно фиксирую границы проверки, см. также записи о браузере и Docker-демоне выше): реальный прогон Lighthouse (нет Chrome/браузерного инструмента в этой сессии); визуальная проверка вёрстки на 360/768/1440px в реальном браузере; онлайн-валидатор schema.org (нет доступа с этой машины к внешнему валидатору, сделана эквивалентная структурная проверка вручную).

**Почему:** раздел 16 требует явного отчёта о выполнении чек-листа — часть пунктов проверяется только реальным браузером/Lighthouse, которых нет в этом окружении; пользователь должен перепроверить именно эти пункты перед стартом платной рекламы (см. также чеклист в `НАСТРОЙКА.md`, раздел 19.7).

## 2026-08-15 — Этапы 4–8 раздела 18 объединены с этапами 2–3

**Решение:** «Главная страница целиком» (4), «Калькулятор (фронт)» (5), «Страницы услуг/районов/прочие» (6), «Блог» (7) и «SEO: JSON-LD, sitemap, robots, OG-картинки» (8) фактически реализованы одновременно с этапами 2 и 3, а не отдельными последовательными итерациями: шаблон и контент для него неразделимы (нельзя осмысленно сверстать `home.html` без готового `pages.json`, калькулятор — без `pricing.json` и `calculator.json`, и т.д.), поэтому весь фронтенд-слой писался и проверялся как единое целое, затем зафиксирован двумя коммитами (движок в 2, контент в 3). Дальнейшие коммиты (с этапа 9) соответствуют разделу 18 один в один.

**Почему:** после коммита этапов 2–3 весь сайт уже собирается и проходит автоматическую проверку (ссылки, JSON-LD, title/description/canonical/h1) на всех 69 страницах × 3 языка — то есть требования этапов 4–8 уже фактически выполнены и не требуют отдельных «пустых» коммитов поверх уже закоммиченного кода.

## 2026-08-16 — Добавлен `psycopg2-binary` в `api/requirements.txt`

**Решение:** в `api/requirements.txt` рядом с `psycopg[binary]==3.2.3` (psycopg v3) добавлен `psycopg2-binary==2.9.10`.

**Почему:** деплой на Render упал с `ModuleNotFoundError: No module named 'psycopg2'`. Причина — `DATABASE_URL`, который отдаёт Render через `fromDatabase.connectionString` (см. `deploy/render.yaml`), приходит в виде обычного `postgresql://...` без суффикса диалекта, а SQLAlchemy 2.0 по умолчанию резолвит схему `postgresql://` в диалект `psycopg2`, а не `psycopg` v3 (тот требует явного `postgresql+psycopg://`). Раз мы не переписываем URL, который формирует сам Render, надёжнее держать оба драйвера установленными: `psycopg2-binary` — потому что его понимает SQLAlchemy без изменений в конфиге по умолчанию для стандартной `postgresql://` строки, `-binary` — чтобы не требовались системные библиотеки сборки (`libpq-dev` и т.п.) в Docker-образе (`python:3.12-slim`). `psycopg` v3 оставлен как есть — им явно пользуется `docker-compose.yml`/`docker-compose.prod.yml`, где URL собирается вручную с префиксом `postgresql+psycopg://`.

Проверено: `pip install psycopg2-binary==2.9.10` ставится чисто на `python:3.12`-совместимом окружении без ошибок сборки, `import psycopg2` работает, полный набор `api/tests` (45/45) зелёный после добавления зависимости.

## 2026-08-16 — Реальные данные владельца: контакты, тарифы, абонемент на регулярную уборку

**Решение:** обновлены `site/content/config.json` (`company_name` → «All Clean», `phone`/`whatsapp` → `+996 505 766 665`, `domain` → `allclean.kg`) и `site/content/pricing.json` (базовые ставки: поддерживающая 80, генеральная 150, после ремонта 170, после переезда 180 сом/м²; `min_order` → 9000 сом). Допуслуги: мытьё окон переведено с «за створку» (400 сом) на «за м²» (owner дал диапазон 300–350, в `pricing.json` записана середина — 325, в маркетинговых `price_from`/FAQ показан минимум диапазона — 300); химчистка дивана переведена с «за посадочное место» (900 сом/место) на плоскую цену за диван (owner дал диапазон 450–750 → середина 600 в расчёте, 450 в `price_from`); добавлены новые допуслуги — химчистка матраса 1-сп./2-сп. (1500/2000 сом, фиксированные, без диапазона) и мойка фасадов (owner дал диапазон 120–200 → середина 160, в `price_from` фигурировал бы 120, отдельной страницы услуги под фасады не создавалось — только пункт в калькуляторе/таблице допуслуг). Ключи допуслуг переименованы `windows_per_sash`→`windows_per_m2`, `sofa_seat`→`sofa_dry_clean`, из `ADDONS_WITH_QTY` (`calculator.js`, `calculator.html`) убран `sofa_seat` (теперь это флажок без количества) и добавлен `facade_wash_per_m2`.

Регулярная уборка: по итогам явного уточнения у владельца (см. AskUserQuestion в диалоге) подтверждено, что 12000/8000/5000 сом — это **фиксированная абонентская плата в месяц**, а не проценты скидки от разовой уборки, как было устроено раньше. Это потребовало замены модели скидок на новую: `pricing.json.discounts` (проценты) заменён на `subscriptions_monthly` (фиксированные суммы) + `visits_per_month` (для расчёта «вы экономите X» — сравнение с оплатой N разовых уборок по обычной цене). И `api/app/pricing.py:compute_price()`, и `site/static/js/calculator.js:computeTotal/computeRange` теперь при `frequency in subscriptions_monthly` полностью игнорируют площадь/допуслуги/срочность/тип объекта и возвращают фиксированную сумму без разброса «от-до» (`price_min == price_max == total`) — потому что это абонемент с объявленной ценой, а не оценка.

Все три языка (`ru`/`ky`/`en`) синхронно обновлены в `calculator.json` (лейблы/юниты допуслуг), `pages.json` (`ceny`: «Скидки за регулярность» → «Абонемент на регулярную уборку», колонка «Скидка» → «Цена абонемента в месяц»), `services.json` (`price_from`, `meta_title`, FAQ о скидке/абонементе, FAQ о цене окон «за створку»→«за м²», `calculator_preset.highlight_addon`), блог-статья про генеральную уборку (иллюстративный пример диапазона цены пересчитан под новые ставки: 80м² generalnaya × 150 сом = 12000, floor не сработал → диапазон 10200–13800 сом; упоминание «за створку» заменено на «за м²»).

**Почему:** реальные бизнес-данные от владельца должны быть единственным источником правды (раздел 19.1 BRIEF.md) и не могут быть кое-как «впихнуты» в старую схему скидок — там, где формат владельца прямо противоречил существующей модели (абонемент вместо процентной скидки), я уточнил через AskUserQuestion вместо угадывания, потому что ошибка здесь — это неверная цена, которую увидит реальный клиент. Диапазоны цен (окна/диван/фасады) переведены в точечные значения для расчёта через середину диапазона (стандартная практика при переходе от «вилки» к единой формуле), а в маркетинговых `price_from` — через минимум диапазона (это поле по смыслу всегда «от»).

**Проверено:** `api/tests` — 45/45 (пересчитаны вручную ожидаемые суммы в `test_pricing.py`, добавлен `test_subscription_tiers_are_flat_monthly_rates`); `site/tests` — 12/12; `site/check_config.py` — `phone`/`whatsapp`/`domain` больше не считаются плейсхолдерами (остался только `email`, который owner не присылал); `site/build.py` — сборка 69×3 страниц без ошибок; вручную сверен собранный `dist/ru/ceny/index.html` — таблица абонемента, чекбоксы новых допуслуг и `#pricing-data` содержат правильные значения.

**Не сделано (осталось для владельца/следующей сессии):** owner также сообщил, что положил реальные фотографии работ в `site/static/img/placeholders/` (27 файлов) — они НЕ подключены к сайту: имена файлов содержат пробелы и кириллицу (небезопасно для URL, разница в регистре путей между Windows и Linux-хостингом может сломать ссылки на проде), и в текущих шаблонах нет галереи, которая бы их использовала (сейчас `about.html`/`home.html`/`blog_index.html` ссылаются только на 4 фиксированных SVG-плейсхолдера). Это отдельная задача — потребует переименования файлов в латиницу без пробелов, решения, где именно на сайте показывать фото (карточки услуг? отдельная галерея? страница «О нас»?), и, скорее всего, сжатия исходников (часть PNG весит 2+ МБ). В этот раз не трогал, так как явного запроса «встрой фото в сайт» не было — только информация о том, что они добавлены в папку.

**Домен:** owner назвал домен «all_clean.kg», но знак подчёркивания недопустим в доменных именах (не проходит `DOMAIN_RE` в `check_config.py`, и часть браузеров/сервисов такой адрес не примет) — записано как `allclean.kg` без подчёркивания, отмечено в `НАСТРОЙКА.md`. Домен ещё не куплен (owner: «домен чуть позже купим») — до покупки сайт продолжит собираться с этим именем в canonical/OG-тегах; это не блокирует dev-сборку, а прод всё равно заблокирован незаполненным `email`.

## 2026-08-16 — Реальные фото владельца подключены к разделам сайта; отзывы — намеренно не тронуты

**Решение:** 27 фото, которые владелец положил в `site/static/img/placeholders/` с описательными русскими именами («Генеральная уборка 1.PNG», «После ремонта1.PNG» и т.д.), пережаты (Pillow: макс. сторона 1600px, JPEG q82) и переименованы в латиницу без пробелов, перенесены в новую папку `site/static/img/gallery/` (в `site/lib/assets.py` весь `img/` копируется как есть, так что вложенная папка работает без изменений в пайплайне). Итог: 25,4 МБ → 3,7 МБ. Подключены как галереи по 3–5 фото в четырёх `services.json` (все 3 языка): «Генеральная уборка», «После ремонта», «Химчистка мебели» — соответствие один в один по названию; «Мойка фасадов и стекол» — на страницу «Мытьё окон» (`mytyo-okon`), так как отдельной страницы под мойку фасадов не существует, а тематически это ближе всего. Добавлена секция `service.gallery` в `service.html` (сетка `grid-4`, переиспользует существующий класс `.hero__media`). «Процесс» (4 фото) — на страницу «О нас» (`о-nas`) новой секцией `page.process_gallery` перед блоком принципов работы.

**Отзывы (`Отзывы1–5.jpeg`) НЕ подключены к текущим карточкам отзывов** — они пережаты и лежат готовые в `site/static/img/gallery/review-1..5.jpg`, но не привязаны к `site/content/{ru,ky,en}/reviews.json`. Причина: все текущие записи в `reviews.json` помечены `"placeholder": true` — это демо-текст, а не реальные отзывы клиентов (раздел 19.7 НАСТРОЙКА.md прямо требует их заменить до старта рекламы). Прикрепить реальное фото человека к выдуманному тексту отзыва — значит выдать вымышленный отзыв за подлинный, что запрещено и правилами Google (см. НАСТРОЙКА.md, «Чего нельзя делать»), и является недобросовестной практикой в принципе. Фото подготовлены и ждут реальных текстов отзывов от владельца.

**Почему:** явная инструкция владельца («добавь фото к каждому разделу, я описал в названии что за что отвечает») выполнена буквально для всех категорий, где подключение фото не создаёт ложного впечатления — но не для отзывов, где текст под фото всё ещё фиктивный. Здесь я сознательно отступил от буквального выполнения ради целостности сайта; решение зафиксировано, а не выполнено молча, чтобы владелец мог поправить, если не согласен, прислав реальные тексты отзывов от людей на фото.

**Проверено:** `site/tests` — 12/12 зелёные; `site/build.py` — 69×3 страниц собираются без ошибок; вручную сверены собранные `dist/ru/uslugi/generalnaya-uborka/`, `dist/ru/uslugi/mytyo-okon/`, `dist/ru/o-nas/` — все пути `img/gallery/*.jpg` резолвятся в реально существующие файлы.

## 2026-08-16 — Полная смена визуальной палитры: спокойная бирюза → яркая красный/жёлтый/зелёный/синий

**Решение:** владелец запросил уход от бирюзово-коралловой палитры BRIEF.md раздела 4 к более яркой, «притягивающей» схеме на основе 4 цветов. Полностью переписаны CSS-токены в `site/static/css/main.css` (light + dark режимы), с сохранением семантики имён переменных:

- **`--color-primary-*`** — раньше «глубокая бирюза», теперь нейтральный тёмный ink (#12151b…#eef0f5, без цветового оттенка). Это осознанный выбор, а не «пятый цвет»: если сделать primary одним из 4 ярких цветов, весь текст/шапка/футер/иконки автоматически станут таким же цветом, что и акценты — получится ровно тот эффект «детского конструктора», от которого предостерегал владелец. Primary держит форму сайта нейтральной, чтобы яркость могла «выстрелить» именно там, где она уместна.
- **`--color-accent-*`** — единственный неизменный цвет CTA-кнопок по всему сайту: насыщенный синий (#2563eb / #1d4ed8 hover). Выбран из 4 цветов не случайно: красный конфликтует по смыслу с `--color-danger` (ошибки форм), жёлтый физически даёт низкий контраст с белым текстом на кнопке, зелёный конфликтует с `--color-success`. Синий — единственный из четырёх, который не пересекается по смыслу ни с одним семантическим цветом (успех/ошибка/предупреждение) и даёт устойчиво хороший контраст.
- **Новые токены `--color-brand-{red,yellow,green,blue}`** (+ пары `-glyph` и `-100`) — для декоративного использования (иконки-бейджи, звёзды рейтинга), НЕ для текста и НЕ для кнопок. У каждого «-glyph» — затемнённый тон, специально подобранный так, чтобы держать контраст ≥4.5:1 на своей же бледной «-100» подложке (проверено вручную по формуле WCAG, см. ниже).
- **`--gradient-brand`** — диагональ красный→жёлтый→зелёный→синий, используется только в hero, финальном CTA и как тонкая 4px декоративная полоса-разделитель (граница футера сверху, через `border-image`). Больше нигде на сайте градиент не встречается — сознательно не «заливал» им карточки услуг целиком (это дало бы 6 разноцветных кричащих карточек на одном экране, что и есть предостережение владельца про «неравнозначность 4 цветов одновременно»); вместо этого палитра проявляется точечно — как цвет бейджа иконки на карточке (см. ниже).
- **Нейтралы** (`--color-bg`, `--color-surface`, `--color-text` и т.д.) — сдвинуты с тёплых бежевых тонов на холодные, с лёгким синим оттенком, чтобы гармонировать с новым синим accent/primary (не «дефолтный серый», а осознанно подобранный под палитру, как и просит хороший дизайн).

**Контраст (WCAG, посчитано по формуле относительной яркости, не на глаз):**
- Текст/фон: 16.05:1; текст-muted/фон: 5.62:1; белый на CTA-синем: 5.17:1; белый на primary-900: 18.28:1 — все с большим запасом выше AA (4.5:1).
- Самое узкое место — белый текст поверх градиента в hero/final-cta на участке жёлтого (самый светлый цвет из четырёх). Исходный жёлтый (#fbbf24) с тёмной подложкой 32% давал только 3.36:1 — недостаточно для обычного текста. Подобрал более тёмный/золотистый жёлтый (#eaa300 вместо чистого лимонного) и усилил тёмную подложку до 42% (`--gradient-overlay`) — итоговый контраст на желтом участке 5.28:1, с запасом выше AA.
- Бейджи иконок (glyph на своей «-100» подложке): красный 5.28:1, жёлтый 4.55:1, зелёный 4.72:1, синий 5.38:1 — все выше 4.5:1.

**Применение палитры по сайту:**
- **Hero** — фото убрано полностью (владелец сам это запросил: «фото человека на стремянке выглядит буднично»). Вместо фото — декоративная inline SVG-иллюстрация: белые полупрозрачные «пузыри» разного размера с редкими цветными акцентами (по одной точке каждого из 4 цветов) и центральным белым медальоном с иконками метлы/сверкающих звёзд — тема «чистота», без фото людей, с лёгкой анимацией всплытия (отключается через `prefers-reduced-motion`).
- **Финальный CTA** (перед футером) — тот же градиент, что и в hero.
- **Иконки-бейджи** карточек услуг и блока «Почему мы» — циклически перебирают 4 цвета через `:nth-of-type(4n+N)`, в виде бледной подложки + затемнённого глифа (не сплошная яркая заливка) — то есть все 4 цвета физически присутствуют на странице одновременно, но как маленькие акценты 40–52px, а не как доминирующие блоки.
- **Футер** — верхняя граница 4px через `border-image: var(--gradient-brand)` — единственный «разделитель» с полным градиентом; само тело футера осталось тёмным нейтральным (primary-900), чтобы не ставить два ярких градиентных блока подряд (final-cta сразу над ним) — это и есть чередование «ярко / спокойно», которое просил соблюсти владелец.
- **Звёзды рейтинга отзывов** — перекрашены с accent (синий, теперь зарезервирован только под CTA) на `--color-brand-yellow-glyph` — золотой/жёлтый цвет для звёзд гораздо привычнее пользователю, чем синий.
- **OG-изображения** (`site/lib/ogimage.py`, генерируются на сборке) — палитра фона сменена с бирюзового градиента на нейтральный тёмный ink-градиент с той же 4-цветной полосой-разделителем внизу (14px), что и в футере сайта — визуальная связка между соцсетевым превью и самим сайтом.
- **Оставшиеся статичные плейсхолдер-SVG** (`about-team.svg`, `about-equipment.svg`, `blog.svg`, favicon, `generate_placeholder_svg()`) — перекрашены с бирюзы на новую нейтрально-синюю схему, чтобы нигде на сайте не осталось следов старой палитры.

**Что НЕ сделано специально (сознательное решение, не забыл):**
- Не заливал градиентом карточки услуг целиком, «шапку» сайта (`.site-header`) и мобильную нижнюю панель — это самые часто видимые/при скролле элементы, яркий фон там утомлял бы глаз и мешал читать навигацию; вместо этого они остались на нейтральных токенах (surface/primary-ink).
- Не делал 4 разных цвета у кнопок CTA по страницам — ровно то, о чём явно просил владелец («узнаваемый постоянный цвет»).

**Проверено:** контраст — вручную по формуле WCAG (см. числа выше, не «на глаз»); `site/tests` — 12/12 зелёные; `site/build.py` — сборка 69×3 страниц без ошибок; собранный `og.png` открыт и визуально проверен — читаемый белый текст на тёмном фоне, градиентная полоса снизу не мешает тексту; для целостной визуальной проверки собран превью-артефакт из реального HTML-вывода главной страницы (hero/услуги/«почему мы»/final-cta/футер) на реальном CSS и шрифте сайта — доступен по ссылке в диалоге.

## 2026-08-16 — Критический баг: название компании невидимо в тёмной теме браузера

**Решение:** владелец сообщил «не видно даже название компании». Причина — architectural: `--color-primary-600/700/900` использовались в CSS сразу в двух несовместимых ролях — как ЦВЕТ ТЕКСТА (лого, пункты меню, хлебные крошки, ссылки в статьях, и т.д. — 15+ мест) и как ФОН (кнопки, футер, подвал калькулятора — 7 мест, всегда с белым текстом поверх). Токен не был переопределён для тёмной темы вообще — то есть в светлой теме это тёмно-чернильный цвет, и в тёмной теме браузера он остаётся ровно тем же тёмно-чернильным цветом. Для фоновых случаев это нормально (тёмный фон + белый текст работает в любой теме), но для текстовых — катастрофа: например, лого в шапке (`color: var(--color-primary-900)`) на фоне шапки (`--color-surface`, которая в тёмной теме сама становится тёмной) даёт контраст **1.03:1** — фактически невидимо. Посчитано формулой WCAG, не на глаз.

Фикс — разделил роли:
1. Все 7 фоновых случаев (`.skip-link`, `.btn-primary`, `.btn-primary:hover`, `.logo__mark`, `.step::before`, `.calc-result`, `.site-footer`) переведены на литеральные hex-значения (`#1e2230`, `#12151b`) вместо переменных — эти фоны обязаны оставаться тёмными в любой теме, раз на них лежит белый текст.
2. Отдельно нашёл и починил ещё 2 скрытых случая той же ловушки: `.hero/.final-cta .btn-accent` (белая кнопка с текстом `--color-primary-900`) и `.btn-whatsapp` (зелёная кнопка с тем же текстом) — их фон ВСЕГДА фиксированный (белый / зелёный WhatsApp-бренд) независимо от темы сайта, поэтому текст на них тоже обязан быть литералом, а не переменной — иначе при попытке «светлить» переменную для читаемости текста в шапке эти две кнопки сломались бы в обратную сторону (светлый текст на белом/зелёном фоне). Тоже переведены на литералы.
3. Для оставшихся 15+ по-настоящему текстовых случаев (лого, `.header-actions__phone`, `.eyebrow`, `.main-nav a:hover`, `.service-card__link`, `.why-card__icon`, `.review-card__avatar`, `.price-table th`, хлебные крошки, ссылки в статьях блога и т.д.) добавил переопределение `--color-primary-500/600/700/900` в `@media (prefers-color-scheme: dark)` — в тёмной теме эти токены теперь светлеют (#7c88a3…#eef0f5), сохраняя контраст 4.94–16.81:1 против тёмного фона (посчитано).

**Почему это не поймали раньше:** превью-артефакт из предыдущего коммита не включал `<header>` (я вырезал только hero/услуги/футер) — баг был технически введён (точнее, резко усугублён — при старой бирюзовой палитре тот же архитектурный изъян давал более тусклый, но не полностью нулевой контраст) ещё на прошлом шаге, но не был визуально пойман, пока владелец не открыл реальный сайт в браузере с тёмной темой ОС.

**Проверено:** контраст лого в тёмной теме — было 1.03:1, стало 15.56:1 (посчитано по формуле WCAG). `site/tests` — 12/12; `build.py` — чисто; собран обновлённый превью-артефакт, теперь включающий шапку сайта и переключатель светлая/тёмная/системная тема прямо в интерфейсе превью — ссылка в диалоге.

## 2026-08-16 — Кнопки +/- санузлов, заглушки на «О нас», блог-превью, реальный логотип

**Кнопки +/- (счётчик санузлов) невидимы:** та же категория бага, что и с логотипом, но в другом месте. Глобальный сброс стилей `input, select, textarea { color: inherit }` явно наследовал цвет текста, а `button { font-family: inherit }` — нет. Из-за этого `.calc-stepper button` (и заодно переключатель языка `.lang-switch__button`, и гамбургер-меню `.nav-toggle`) полагались на цвет кнопки браузера по умолчанию, а не на палитру сайта. Фикс — один класс `color: inherit` на глобальном `button` в ресете (main.css), чинит разом все три места и защищает от того же бага в будущем для любой новой кнопки.

**«О нас» — убраны текстовые заглушки-фото:** секции «Наша команда» и «Оборудование и химия» показывали SVG-плашки с рамкой и подписью — раньше это было честно (плейсхолдер и есть плейсхолдер), но по факту это визуально выглядело как сломанные картинки. Владелец подтвердил — реальных фото команды/оборудования нет. Убрал `<img>` из обеих секций, макет стал текстовым (2 колонки текста вместо текст+фото), поля `image`/`image_alt` вычищены из `pages.json` на всех 3 языках как мёртвые данные, сами SVG-файлы (`about-team.svg`, `about-equipment.svg`) удалены. Попутно удалён и `hero.svg` — он перестал использоваться ещё в прошлой правке (hero заменён на декоративную иллюстрацию), но файл забыл удалить тогда.

**Блог-превью — «вставь из интернета»:** технически не могу скачивать произвольные картинки из интернета (нет инструмента загрузки файлов по URL), и не стал бы использовать случайное фото без ясной лицензии на коммерческом сайте — риск нарушения авторских прав. Вместо этого — собственная декоративная SVG-иллюстрация: мягкий пастельный градиент из тех же 4 цветов бренда + иконка документа/статьи. Смотрится как осознанное оформление, а не как явная заглушка «сюда вставить фото».

**Реальный логотип:** во время работы в `site/static/img/gallery/` неожиданно появился `logotip.jpeg` — настоящий логотип компании (лист/веник/силуэт человека на синем фоне, «all clean», «Клининговая компания»). Так как это прямо отвечает на только что найденный баг «не видно название компании», подключил без дополнительных вопросов: обрезал/пережал в `site/static/img/logo.png` (232×240) для шапки сайта — заменил абстрактную SVG-иконку + текст `{{ config.company_name }}` на реальное изображение логотипа; отдельно вырезал только иконку-кластер (без текста) в `site/static/icons/favicon.png` (180×137) — заменил прежний рисованный фавикон-«звёздочку» на настоящий бренд-значок. Старый `favicon.svg` удалён, оригинал `logotip.jpeg` удалён из `gallery/` (не рабочее фото, а логотип — уже обработан в нужные форматы).

**Проверено:** `site/tests` — 12/12; `build.py` — чисто; в собранном `dist/ru/index.html` — `<img class="logo__image" src="/img/logo.png">` и `<link rel="icon" href="/icons/favicon.png">` подтверждены напрямую.
